# @tickstem/heartbeat

## 0.2.0

### Minor Changes

- 65673be: Add `@tickstem/heartbeat` package for dead-man's switch heartbeat monitoring. Create, list, get, update, pause, resume, and delete heartbeats. Ping a heartbeat token after each successful job run — no API key required, the token is the credential. Query ping history per heartbeat.
