---
"@tickstem/uptime": minor
---

Add `@tickstem/uptime` package for managing HTTP uptime monitors. Create, list, get, update, pause, resume, and delete monitors. Query check history with per-check status, duration, and error details.
